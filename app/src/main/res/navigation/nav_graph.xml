<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.todo.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_homeFragment_to_addTaskFragment"
            app:destination="@id/addTaskFragment" />

        <action
            android:id="@+id/action_homeFragment_to_viewTaskFragment"
            app:destination="@id/viewTaskFragment" />
    </fragment>

    <fragment
        android:id="@+id/addTaskFragment"
        android:name="com.example.todo.ui.addtask.AddTaskBottomSheet"
        android:label="Add Task"
        tools:layout="@layout/bottom_sheet_add_task" />

    <fragment
        android:id="@+id/viewTaskFragment"
        android:name="com.example.todo.ui.viewtask.ViewTaskFragment"
        android:label="View Task"
        tools:layout="@layout/fragment_view_task">

        <argument
            android:name="taskID"
            app:argType="integer"
            android:defaultValue="-1" />

        <action
            android:id="@+id/action_viewTaskFragment_to_homeFragment"
            app:destination="@id/homeFragment" />
    </fragment>

</navigation>